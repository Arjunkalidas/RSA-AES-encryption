'use strict'

// TODO: Encryption sequence
    // Invoke RSAEncrypt for generating and encrypting a 16, 24 or 32 byte key
    // Invoke AES Encrypt to encrypt the payload using the key before encryption
    
// TODO: Decryption sequence
    // Invoke RSADecrypt to decrypt the 16, 24 or 32 byte key
    // Invoke AESDecrypt to decrypt the payload using the decrypted key